
export default class TemplateTileBlob {
  constructor(parameters) {
    this.bitmap = parameters.bitmap;
    this.tileCoords = parameters.tileCoords;
    this.pixelCoords = parameters.pixelCoords;
  }
}